# Local Sites preview. Set the verified origin in Sites runtime when publishing there.
# build:pages uses https://scientific-parallax.com unless overridden in the command environment.
SITE_ORIGIN=http://localhost:3000
